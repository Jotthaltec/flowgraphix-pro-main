# Vai junto para o servidor: o botão "Run NPM Install" do cPanel roda um
# `npm install` cru, e sem isso ele puxaria vite/typescript/supabase-cli para
# dentro da hospedagem compartilhada à toa. Só as dependências de runtime.
omit=dev
audit=false
fund=false
